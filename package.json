{
    "name": "fretty",
    "private": true,
    "scripts": {
        "build-wasm": "echo 'Skipping wasm build on Vercel. Original command: 'cd wasm/audio_processing && wasm-pack build --target web --out-dir ../../frontend/src/wasm'",
        "dev": "npm run build-wasm && cd frontend && npm run dev",
        "build": "npm run build-wasm && cd frontend && npm run build"
    }
}
